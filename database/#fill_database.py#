from models import Paper, Author
import csv


def main():
    reader = None
    with open('input/data.csv', 'r') as f:
        reader = csv.reader(f)

        columns = reader.next()
        col_to_i = {
            i:col for col in enumerate(columns)
        }
        for row in reader:
            
            raise

if __name__ == '__main__':
    main()
